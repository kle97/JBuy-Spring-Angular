export interface Product {
  id: string,
  brand: string,
  name: string,
  regularPrice: number,
  discountPrice: number,
  stock: number,
  images: string,
  averageRating: number,
  ratingCount: number,
}
