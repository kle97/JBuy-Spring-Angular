import { MatchMediaPipe } from './match-media.pipe';

describe('MatchMediaPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchMediaPipe();
    expect(pipe).toBeTruthy();
  });
});
